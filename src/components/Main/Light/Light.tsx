import LightList from './LightList';
import { LightContainer } from './Light.style';

const Light = () => {
  return (
    <LightContainer>
      <LightList />
    </LightContainer>
  );
};

export default Light;